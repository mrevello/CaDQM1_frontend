import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  componentLabel: {
    fontSize: 10,
    width: '20%',
  },
  itemComponent: {
    width: '100%',
  },
  labeledValue: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
  },
});
